# Initial Handoff Sources

This folder preserves the source context supplied when `homefeed` was created.

Files:

- `homefeed-codex-handoff.md`: product, stack, filesystem, data model, import,
  search, AI, UI, backend module, and milestone context.

Treat this as evidence for extraction. Do not edit it to match later model
decisions; create refined artifacts under `docs/semantics/`, `docs/slices/`,
and `work/changes/` instead.
